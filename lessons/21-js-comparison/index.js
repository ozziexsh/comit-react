
alert("Enter two numbers to see if the first is greater than the second")
const fn=prompt(" Enter first number");
const sn=prompt(" Enter first number");
result=fn >sn;
const result=confirm("were we true",result);
